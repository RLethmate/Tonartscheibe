{
  "name": "improscheibe-app",
  "version": "0.0.1",
  "private": true,
  "description": "Spike: Mikrofonzugriff (getUserMedia/AudioContext) in einer Android-WebView via Capacitor testen",
  "scripts": {
    "sync": "cap sync android",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.0",
    "@capacitor/cli": "^8.4.0",
    "@capacitor/core": "^8.4.0"
  }
}
